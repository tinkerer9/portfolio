{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "types": "wrangler types && astro sync",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.0",
    "@astrojs/markdown-satteri": "^0.4.0",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
